## 🔄 系統資料流程圖 

本章節使用 DFD 圖表來描述語音轉文字系統的資料流向，包含系統邊界與內部程序細節。

### 1. 系統環境圖
```mermaid
graph LR
    %% --- 樣式定義 (DFD 標準風格) ---
    %% 外部實體 (Entity): 方形
    classDef entity fill:#e1f5fe,stroke:#01579b,stroke-width:2px,color:#000;
    %% 系統/程序 (Process): 圓角矩形
    classDef system fill:#fff9c4,stroke:#fbc02d,stroke-width:2px,color:#000;
    
    %% --- 節點定義 ---
    User[外部使用者<br/>User]:::entity
    System((語音轉文字<br/>全棧系統)):::system

    %% --- 資料流向 ---
    %% 輸入
    User -- "1. 上傳音訊檔 / 語音串流" --> System
    User -- "2. 功能請求 (摘要/翻譯)" --> System
    
    %% 輸出
    System -- "3. 逐字稿文本" --> User
    System -- "4. 重點摘要" --> User
    System -- "5. 翻譯結果" --> User
```
### 2.DFD 圖0 
```mermaid
graph TD
    %% --- 樣式定義 ---
    %% 外部實體 (Entity): 藍色方形
    classDef entity fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000;
    %% 程序 (Process): 黃色圓角
    classDef process fill:#fff9c4,stroke:#fbc02d,stroke-width:2px,color:#000,rx:10,ry:10;
    %% 資料儲存 (Data Store): 灰色資料庫形狀
    classDef store fill:#f5f5f5,stroke:#616161,stroke-width:2px,stroke-dasharray: 3 3,color:#000;

    %% --- 1. 外部實體 ---
    User[使用者<br/>User]:::entity

    %% --- 2. 內部程序 (至少三項) ---
    P1(P1. 音訊接收與預處理):::process
    P2(P2. AI 語音識別核心):::process
    P3(P3. 自然語言分析與翻譯):::process
    P4(P4. 結果整合與輸出):::process

    %% --- 3. 資料儲存 ---
    DB1[(D1. 暫存檔案區)]:::store
    DB2[(D2. 轉寫紀錄資料庫)]:::store

    %% --- 4. 資料流向 (Data Flow) ---
    
    %% P1: 接收與 FFmpeg 處理
    User -- "音訊檔 / 錄音" --> P1
    P1 -- "原始音訊" --> DB1
    DB1 -- "讀取音訊" --> P1
    P1 -- "標準化音訊 (WAV 16kHz)" --> P2

    %% P2: WhisperX 轉寫
    P2 -- "呼叫 WhisperX 模型" --> P2
    P2 -- "原始逐字稿 (JSON)" --> DB2
    
    %% P3: 摘要與翻譯
    User -- "請求摘要/翻譯" --> P3
    DB2 -- "讀取逐字稿" --> P3
    P3 -- "呼叫 Transformer 模型" --> P3
    P3 -- "摘要與翻譯文本" --> DB2

    %% P4: 輸出
    DB2 -- "完整資料 (文/摘/譯)" --> P4
    P4 -- "前端顯示格式 (JSON)" --> User
```
* **程序說明 (Process Description)**
>P1. 音訊接收與預處理：負責接收前端上傳的檔案，並利用 FFmpeg 進行格式轉換、降噪與取樣率調整 (Resampling)。

>P2. AI 語音識別核心：將處理過的音訊輸入至 WhisperX 模型，輸出帶有時間戳記的逐字稿。

>P3. 自然語言分析與翻譯：根據使用者需求，調用 Transformer 模型針對逐字稿進行重點摘要提取或多語言翻譯。

>P4. 結果整合與輸出：將所有生成的文字資料包裝成前端可讀的 API 格式 (JSON) 並回傳。
